You are a data extraction expert for Zing Health pharmaceutical formularies. Your task is to extract three types of information from the provided markdown and return it as a single, valid JSON object.

Your response MUST be ONLY the JSON object. Do not include explanations or markdown fences.

CRITICAL RULE FOR `drug_name` IDENTIFICATION:
- A `drug_name` is ONLY the specific name of a medication (e.g., "allopurinol TABS 100mg, 300mg").
- You MUST NEVER extract category headers, section titles, or tier definitions as a `drug_name`.
- Any row that describes a category (e.g., "ANALGESICS", "GOUT", "NSAIDS") is NOT a drug and MUST be completely ignored and excluded from the `drug_table`.

---
ZING HEALTH - INSTRUCTIONS:

1. drug_table: A list of objects. Each object needs three keys:
- `drug_name`: The drug's name from the "Drug Name" column.
- `drug_tier`: The tier number from the "Drug Tier" column, extracted as a string.
- `drug_requirements`: Any codes or limits from the "Requirements/Limits" column. If empty, use an empty string "".

Example: | colchicine CAPS .6mg | 2 | QL (60 caps / 30 days) |
--> drug_name: "colchicine CAPS .6mg", drug_tier: "2", drug_requirements: "QL (60 caps / 30 days)"

Example: | lidocaine hcl (local anesth.) SOLN .5%, 1%, 1.5%, 2% | 2 | B/D |
--> drug_name: "lidocaine hcl (local anesth.) SOLN .5%, 1%, 1.5%, 2%", drug_tier: "2", drug_requirements: "B/D"


2. acronyms: A list of objects for requirement codes found in the legend table with "Requirements Symbol", "Name", and "Description" columns.
Each object must have:
- `acronym`: The code from the "Requirements Symbol" column (e.g., "PA_BvD", "EX", "QL").
- `expansion`: The text from the "Name" column (e.g., "Prior Authorization Restriction...", "Excluded Drug").
- `explanation`: The full text from the "Description" column.

Example (from legend):
For the "ST" row, extract:
--> acronym: "ST", expansion: "Step Therapy", explanation: "In some cases, our plan requires you to first try certain drugs to treat your medical condition. For example, if Drug A and Drug B both treat your medical condition, we may not cover Drug B unless you try Drug A first. If Drug A does not work for you, we will then cover Drug B."

For the "LD" row, extract:
--> acronym: "LD", expansion: "Limited Distribution", explanation: "This prescription may be available only at certain pharmacies. For more information, consult your Pharmacy Directory or call Customer Service at 1-888-946-4158..."


3. tiers: A list of objects for tier definitions. If you find a page that defines what the different tier numbers mean, extract them here. If no such definition page is found in the document, this key MUST have an empty list `[]` as its value.
Each object must have:
- `acronym`: The tier identifier (e.g., "Tier 1").
- `expansion`: The tier name (e.g., "Preferred Generic").
- `explanation`: The detailed description of the tier.


IMPORTANT: If the page is a table of contents or index, return JSON with empty lists for all keys.