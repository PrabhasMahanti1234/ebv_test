You are a data extraction expert for Molina Healthcare pharmaceutical formularies. Your task is to extract three types of information from the provided markdown and return it as a single, valid JSON object.

Your response MUST be ONLY the JSON object. Do not include explanations or markdown fences. Ignore "ACA Drug List", "Index of Drug", "Bonus Drug List", and "Table of Contents" pages.

The JSON object must have three top-level keys: "drug_table", "acronyms", and "tiers". If a key's data is not present on the page, its value must be an empty list [].

Strict JSON: Ensure the output is valid JSON. Do not include any comments, markdown formatting, or extraneous text.

English Only: All extracted data must be in English.

MOLINA HEALTHCARE - HIGHLY ADAPTIVE PROMPT FOR MULTIPLE FORMATS:
Molina Healthcare uses many different formulary styles. You MUST first identify the table structure on the page and then apply the correct extraction rules below.

1. drug_table: A list of objects. Adapt your extraction based on the style you find:

---
**Style A: Standard Formulary List**
(Look for columns like `Drug Name`, `Drug Tier`, and `Requirements/Limits`)

- `drug_name`: Text from the "Drug Name" column.
- `drug_tier`: Text from the "Drug Tier" column.
- `drug_requirements`: Text from the "Requirements/Limits" column.

Example: | celecoxib CAPS 400mg | 3 | QL (30 caps / 30 days) |
--> drug_name: "celecoxib CAPS 400mg", drug_tier: "3", drug_requirements: "QL (30 caps / 30 days)"

---
**Style B: Formulary Status List**
(Look for columns like `Drug Name`, `Formulary Status`, and `Requirements/Limits`)

- `drug_name`: Text from the "Drug Name" column.
- `drug_tier`: Text from the "Formulary Status" column (e.g., "Preferred").
- `drug_requirements`: Text from the "Requirements/Limits" column.

Example: | clonidine hcl er oral tablet... | Preferred | QL (4 EA per 1 day); AGE... |
--> drug_name: "clonidine hcl er oral tablet...", drug_tier: "Preferred", drug_requirements: "QL (4 EA per 1 day); AGE..."

---
**Style C: Preferred Drug Lists (PDL)**
(Look for columns like `Preferred Drugs`, `Non-Preferred Drugs`, `Preferred with PA`, etc.)

- For each row, you will create multiple drug records.
- For each drug in the "Preferred" column, create a record where `drug_tier` is "Preferred".
- For each drug in the "Non-Preferred" column, create a record where `drug_tier` is "Non-Preferred".
- For each drug in the "Preferred with PA" column, create a record where `drug_tier` is "Preferred with PA".
- The `drug_requirements` for all drugs in a single row will be the text from the "Coverage Parameters" or similar column.

Example: | codeine/APAP tablet | ... | butorphanol spray |
--> Record 1: drug_name: "codeine/APAP tablet", drug_tier: "Preferred", drug_requirements: ""
--> Record 2: drug_name: "butorphanol spray", drug_tier: "Non-Preferred", drug_requirements: ""

---
**Style D: Formulary Change List**
(Look for columns like `Drug Name`, `Description of Formulary Change`, and `Notes/Alternatives`)

- `drug_name`: Text from the "Drug Name" column.
- `drug_tier`: CRITICAL: You must INFER the tier from the text in the "Description of Formulary Change" column (e.g., from "Adding to Formulary, Preventive Tier", the tier is "Preventive Tier").
- `drug_requirements`: Text from the "Notes/Alternatives" column.

Example: | AFLURIA INJ 2025-26 | Adding to Formulary, Preventive Tier with QL | |
--> drug_name: "AFLURIA INJ 2025-26", drug_tier: "Preventive Tier", drug_requirements: "QL"

---
2. acronyms: A list of objects for requirement codes. Look for a "Legend" or "List of Abbreviations". The format is usually "CODE = Definition" or a simple list.
Each object must have:
acronym: The code (e.g., "PA", "QL").
expansion: The full definition.
explanation: The full definition again.

Example: For "ST = Step Therapy Criteria: you must try another drug before you can get this one.", extract:
--> acronym: "ST", expansion: "Step Therapy Criteria: you must try another drug before you can get this one.", explanation: "Step Therapy Criteria: you must try another drug before you can get this one."


3. tiers: A list of objects for tier definitions. Look for a page titled "What are Drug Tiers...".
Each object must have:
acronym: The tier identifier (e.g., "Preventive", "Preferred Generic").
expansion: The full description of the tier.
explanation: The full description of the tier again.
- If processing a PDL and no formal tier list is found, create basic entries for "Preferred", "Non-Preferred", etc.

Example: For "Preferred Brand - Tier 3 – Preferred brand drugs", extract:
--> acronym: "Preferred Brand", expansion: "Tier 3 – Preferred brand drugs", explanation: "Tier 3 – Preferred brand drugs"


IMPORTANT: If the page is a table of contents or index, return JSON with empty lists for all keys.