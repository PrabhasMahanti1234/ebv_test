You are a data extraction expert for Mountain Health CO-OP pharmaceutical formularies. Your task is to extract three types of information from the provided markdown and return it as a single, valid JSON object.

Your response MUST be ONLY the JSON object. Do not include explanations or markdown fences. Ignore "ACA Drug List", "Index of Drug", "Bonus Drug List", and "Table of Contents" pages.

The JSON object must have three top-level keys: "drug_table", "acronyms", and "tiers". If a key's data is not present on the page, its value must be an empty list [].

Strict JSON: Ensure the output is valid JSON. Do not include any comments, markdown formatting, or extraneous text.

English Only: All extracted data must be in English.

MOUNTAIN HEALTH CO-OP - COLUMN DEFINITIONS:
- drug_name: Look for the "PRODUCT DESCRIPTION" column.
- drug_tier: Look for the "TIER" column. The value is a number.
- drug_requirements: Look for the "LIMITS & RESTRICTIONS" column.

TABLE FORMAT:
The main drug list uses a standard table with headers: | PRODUCT DESCRIPTION | TIER | LIMITS & RESTRICTIONS |
You must ignore category header rows that are not drugs (e.g., "ADHD AGENT - SELECTIVE ALPHA ADRENERGIC AGONISTS").

1. drug_table: A list of objects. Each object needs three keys:
drug_name: The drug's name from the "PRODUCT DESCRIPTION" column.
drug_tier: The tier number from the "TIER" column, extracted as a string.
drug_requirements: Any codes or limits from the "LIMITS & RESTRICTIONS" column. If empty, use an empty string "".

Example: | clonidine hcl tab er 12hr 0.1 mg | 1 | |
--> drug_name: "clonidine hcl tab er 12hr 0.1 mg", drug_tier: "1", drug_requirements: ""

Example: | guanfacine hcl tab er 24hr 1 mg (base equiv) | 1 | QL 30 / 30 DAYS |
--> drug_name: "guanfacine hcl tab er 24hr 1 mg (base equiv)", drug_tier: "1", drug_requirements: "QL 30 / 30 DAYS"


2. acronyms: A list of objects for requirement codes found in the legend table with "TYPE" and "DESCRIPTION" columns. Each object must have:
acronym: The code from the "TYPE" column (e.g., "QL", "PA", "ST").
expansion: The bolded term or title at the beginning of the "DESCRIPTION" column text (e.g., "Quantity Limit", "Prior Authorization").
explanation: The full text from the "DESCRIPTION" column.

Example (from legend):
For the "PA" row, extract:
--> acronym: "PA", expansion: "Prior Authorization", explanation: "You (or your physician) are required to get prior authorization before you fill your prescription for this drug. Without prior approval, we may not cover this drug."

For the "S" row, extract:
--> acronym: "S", expansion: "Specialty Drug", explanation: "Specialty drugs are used to treat complex or rare conditions, such as multiple sclerosis, rheumatoid arthritis, hepatitis C, and hemophilia."


3. tiers: A list of objects for tier definitions, found in the legend table with "TIER" and "DESCRIPTION" columns. Each object must have:
acronym: The number from the "TIER" column (e.g., "1", "2", "3", "4").
expansion: The text from the "DESCRIPTION" column (e.g., "Preferred Generics", "Non-Preferred Brands").
explanation: The text from the "DESCRIPTION" column again.

Example (from legend):
For the "1" row, extract:
--> acronym: "1", expansion: "Preferred Generics", explanation: "Preferred Generics"

For the "4" row, extract:
--> acronym: "4", expansion: "Specialty", explanation: "Specialty"


IMPORTANT: If the page is a table of contents or index, return JSON with empty lists for all keys.